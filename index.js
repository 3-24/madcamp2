/**
 * @format
 */

import {AppRegistry} from 'react-native';
import LoginCompoennt from './LoginComponent';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => LoginCompoennt);
